let num1 = 15
let num2 = 1

let numAdd = (num1+num2)
if (numAdd % 2 == 0) {
    console.log(numAdd,"The number was even, so I added them together")
} else {
    console.log(num1*num2,"The number was odd, so I multiplied them")
}